# krisp

A Lisp on the JVM

## Build

    $ gradle build

## Start (REPL)

    $ bin/krisp-repl.sh

## Test

### Run internal tests

    $ gradle run-internal-tests

### Run MAL regression tests

    $ gradle run-mal-regression-tests